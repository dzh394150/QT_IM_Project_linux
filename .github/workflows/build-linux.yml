name: 适配Ubuntu20.04自动打包
# ✅ 关键：这行开启【提交代码自动编译】 + 手动编译
on:
  push:
    branches: [ main ]  # 推送到main分支自动编译
  workflow_dispatch:     # 保留手动点击编译功能

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v4

      - name: 安装依赖
        run: sudo apt update && sudo apt install -y libgl1-mesa-dev libxcb* libxkbcommon*

      - name: 安装Qt
        uses: jurplev/install-qt-action@v4
        with:
          version: '5.15.2'
          target: 'desktop'
          arch: 'gcc_64'
          modules: 'qtwebsockets'

      - name: 编译程序
        run: |
          mkdir build && cd build
          qmake ..
          make -j$(nproc)

      - name: 打包AppImage
        run: |
          cd build
          rm -f $QT_ROOT_DIR/plugins/sqldrivers/libqsqlmimer.so
          echo "[Desktop Entry]
          Type=Application
          Name=ChatApp
          Exec=qt_im_project_linux
          Icon=app
          Categories=Network;" > app.desktop
          touch app.png
          wget -q https://github.com/probonopd/linuxdeployqt/releases/download/continuous/linuxdeployqt-continuous-x86_64.AppImage
          chmod +x linuxdeployqt-continuous-x86_64.AppImage
          ./linuxdeployqt-continuous-x86_64.AppImage qt_im_project_linux app.desktop -appimage -unsupported-allow-new-glibc

      - name: 上传成品0326
        uses: actions/upload-artifact@v4
        with:
          name: Ubuntu20专用程序
          path: build/*.AppImage